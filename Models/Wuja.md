# Pvlovski to use the env use 


## `source myenv/bin/activate`

## Install reqs with `pip install -r requirements.txt`


## `python -m Models.Model_test.app` from root - Hacknarock2026 (should work)